# Environment variables for AiTermy

# 1. OpenRouter API Key (REQUIRED)
#    - Get yours at https://openrouter.ai/keys
#    - Must start with 'sk-or-'
OPENROUTER_API_KEY=""

# 2. Preferred AI Model (REQUIRED)
#    - This model will be used for generating responses.
#    - Setup script provides options, but you can specify any valid OpenRouter model ID here.
#    - Examples:
#      google/gemini-2.0-flash-001    # Fast, cost-effective
#      meta-llama/llama-4-scout      # More capable, cost-effective
#      anthropic/claude-3.7-sonnet   # Very capable, more expensive
#      google/gemini-2.5-pro-preview-03-25  # Balanced
OPENROUTER_MODEL="google/gemini-2.5-pro-preview-03-25"

# 3. Command Name (Set by setup.sh)
#    - The command you will use to invoke AiTermy (e.g., 'ai', 'termy').
#    - This is set automatically during the setup process.
# COMMAND_NAME="ai" # Example, do not uncomment unless manually configuring

# 4. Logging Configuration (OPTIONAL)
#    - Set LOGGING_ENABLED to "true" to log requests, responses, and errors.
#    - Useful for debugging.
LOGGING_ENABLED="false"

# 5. Log File Location (OPTIONAL - only used if LOGGING_ENABLED is true)
#    - Absolute path or path relative to the user's home directory (using ~).
#    - The directory will be created if it doesn't exist.
LOG_FILE="~/.aitermy/logs/aitermy.log"
